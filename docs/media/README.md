# Media

- `wattless-dashboard.webp`: captura real del dashboard generada desde el stack local de producción.
- `wattless-dashboard-report.json`: respuesta del escáner usada para generar esa captura.

Queda pendiente capturar un GIF corto del flujo completo una vez la demo pública esté desplegada en CubePath.
